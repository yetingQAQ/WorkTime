[project]
name = "worktime"
version = "0.1.0"
description = "工作时间进度条程序 - 在屏幕顶部显示工作进度，到下班时间自动关机"
requires-python = ">=3.10"
dependencies = [
    "pyqt6>=6.4.0",
]

[project.optional-dependencies]
build = [
    "pyinstaller>=6.0.0"
]
